import 'package:flutter/material.dart';

class AppColor{
  static Color progressIndcatorColor = Colors.black; 
}

// lib/theme/constants.dart


class ThemeConstants {
  // Primary colors
  static const primaryBlue = Color(0xFF2196F3);
  static const secondaryBlue = Color(0xFF64B5F6);
  static const lightBlue = Color(0xFFE3F2FD);
  
  // Background colors
  static const backgroundColor = Colors.white;
  static const surfaceColor = Colors.white;
  
  // Text colors
  static const primaryTextColor = Color(0xFF2C3E50);
  static const secondaryTextColor = Color(0xFF7F8C8D);
  
  // Border and shadow colors
  static const borderColor = Color(0xFFE0E0E0);
  static const shadowColor = Color(0x1A000000);
  
  // Status colors
  static const successColor = Color(0xFF4CAF50);
  static const errorColor = Color(0xFFF44336);
  static const warningColor = Color(0xFFFFC107);
  
  // Spacing
  static const double defaultPadding = 16.0;
  static const double smallPadding = 8.0;
  static const double largePadding = 24.0;
  
  // Border radius
  static const double borderRadius = 12.0;
  static const double smallBorderRadius = 8.0;
  static const double largeBorderRadius = 16.0;


  static const borderBlue = Color(0xFFBDDCFF);
  static const darkBlue = Color(0xFF1557B0);
}